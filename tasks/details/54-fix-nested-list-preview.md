# 54-fix-nested-list-preview

- 编号：54

## 实现与验证

- 列表解析按原始行缩进维护层级栈，生成嵌套的 ul/ol，支持类型切换、多级回退和 Tab 缩进。
- 列表间空行保留层级，兼容 Turndown 回写；子列表底部间距收紧。
- 源码锚点只放在最外层列表，保持整块编辑回写和位置映射约定。
- 在 Tests/AppTests.swift 补充父子分组、混合列表、多级回退、空行、Tab 和 UTF-16 偏移回归用例。
- 已检查解析分支和 HTML 闭合逻辑；按用户要求未运行编译或测试，也未运行 SwiftLint。

# 58-fix-thematic-breaks

- 编号：58

## 实现与验证

- 在列表识别前统一匹配同类三个及以上的星号、横线、下划线，允许标记间空格和 Tab，行首最多三个空格。
- 保留每条分隔线独立的 HTML 节点和源码锚点，连续分隔线不会合并。
- 新增四组回归用例，覆盖常用写法、连续五条分隔线、LF/CRLF 偏移、非法标记、代码保护和列表边界。
- 源码及测试文件的 git diff --check 通过。按用户要求未编译、未运行测试或 SwiftLint。
- 本次仅补齐分隔线匹配，未扩展 Setext 标题及容器内块级解析。
